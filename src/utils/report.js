export function resultStatus(value, range) { const n = Number(value); if (!value || Number.isNaN(n)) return 'pending'; const nums = range.match(/\d+(?:\.\d+)?/g)?.map(Number) || []; const min = nums[0], max = nums[1]; if (min === undefined || max === undefined) return 'normal'; if (n < min * .7 || n > max * 1.5) return 'critical'; if (n < min) return 'low'; if (n > max) return 'high'; return 'normal'; }
export const statusStyle = { normal:'bg-emerald-50 text-emerald-700', low:'bg-red-50 text-red-700', high:'bg-amber-50 text-amber-700', critical:'bg-rose-100 text-rose-800', pending:'bg-slate-100 text-slate-500' };
