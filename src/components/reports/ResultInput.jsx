import { resultStatus, statusStyle } from '../../utils/report';
export default function ResultInput({ parameter, value, onChange }) { const status=resultStatus(value,parameter.range); return <div className="flex items-center gap-2"><input aria-label={`${parameter.name} result`} value={value} onChange={event=>onChange(event.target.value)} className="w-28 rounded-lg border border-blue-200 bg-blue-50/40 px-2.5 py-2 font-mono text-sm font-medium outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100" placeholder="Enter"/><span className={`rounded-full px-2 py-1 text-[10px] font-bold uppercase ${statusStyle[status]}`}>{status}</span></div>; }