import { FiCheck, FiSearch } from 'react-icons/fi';
export default function TestSearch({ query, onQueryChange, tests, onSelect }) { return <section className="card p-5"><div className="mb-4"><h2 className="font-semibold">Search laboratory test</h2><p className="mt-1 text-xs text-slate-400">Select a test to load its predefined laboratory template instantly.</p></div><div className="relative"><FiSearch className="absolute left-3 top-3 text-slate-400"/><input className="field pl-9" value={query} onChange={event=>onQueryChange(event.target.value)} placeholder="Search CBC, LFT, KFT or any laboratory test..."/>{query && <div className="absolute z-20 mt-1 max-h-60 w-full overflow-auto rounded-xl border bg-white p-1 shadow-xl">{tests.length ? tests.map(test => <button key={test.id} onClick={()=>onSelect(test)} className="flex w-full items-center justify-between rounded-lg px-3 py-2.5 text-left hover:bg-blue-50"><span><span className="block text-sm font-medium text-slate-700">{test.name}</span><span className="text-xs text-slate-400">{test.code} · {test.department}</span></span><span className="grid h-5 w-5 place-items-center rounded-full border border-blue-200 text-blue-600"><FiCheck size={12}/></span></button>) : <p className="p-3 text-sm text-slate-400">No matching test found.</p>}</div>}</div></section>; }